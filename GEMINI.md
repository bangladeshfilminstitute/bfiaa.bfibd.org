# Core Directives

1. **Never delete anything without asking the user first.** 
   When editing, updating, or replacing sections of code, you must ensure that surrounding blocks (events, sections, cards, UI elements, etc.) are meticulously preserved unless the user explicitly instructs you to delete them. If in doubt, ask for permission before removing content.
